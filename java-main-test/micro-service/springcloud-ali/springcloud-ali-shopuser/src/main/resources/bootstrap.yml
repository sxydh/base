spring:
  application:
    name: service-shopuser
  cloud:
    nacos:
      config:
        # nacos中心地址
        server-addr: 127.0.0.1:8848
        # 配置文件格式
        file-extension: properties
  profiles:
    # 环境标识
    active: dev